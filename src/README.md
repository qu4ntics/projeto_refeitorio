# ReservaIF

A documentação completa do projeto — descrição, instruções de instalação e execução — está no [README da raiz do repositório](../README.md).

Todos os comandos (`manage.py`, `pip install`, etc.) devem ser executados a partir desta pasta (`src/`).
